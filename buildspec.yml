version: 0.2

phases:
  install:
    commands:
      - pip install pytest  # Installiere pytest
  build:
    commands:
      - pytest test.py  # Führe die Tests aus

artifacts:
  files:
    - '**/*'
